# 🎬 Story Lens

Story Lens is a local AI-powered storyboard generator. Just enter a movie idea, and it will generate scenes with descriptions and images using open-source models.

## Features
- No API keys required
- Local text generation (GPT-2)
- Local image generation (Stable Diffusion)
- Streamlit interface

## Setup
```bash
pip install -r requirements.txt
streamlit run app.py
